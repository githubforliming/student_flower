package rules;

// 引入实体
import com.example.droolstest02.entity.TestNoneEntity;

// 编写规则
rule "att_no_loop_0"
    no-loop true
    when
        $test:TestNoneEntity(str == "noloop01")
    then
        System.out.println("noloop01");
        Thread.sleep(1000);
        update($test)
end
