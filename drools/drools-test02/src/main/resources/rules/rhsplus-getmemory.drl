package rules;

// 引入实体
import com.example.droolstest02.entity.TestNoneEntity
import org.drools.core.WorkingMemory;



rule "rhsplus_getworkingmemory01"
    when
        $test01:TestNoneEntity(i > 10)
    then
        WorkingMemory workingMemory = drools.getWorkingMemory();
        System.out.println(workingMemory);

end